{
    "pdf_folder": "${PDF_FOLDER}",
    "mysql_conn": "mysql+pymysql://${DB_USER}:${DB_PASSWORD}@localhost:3306/dev_night_audit",
    "email": {
        "sender": "${EMAIL_SENDER}",
        "receiver": "${EMAIL_RECEIVER}",
        "smtp_server": "smtp.gmail.com",
        "smtp_port": 587,
        "username": "${EMAIL_USER}",
        "password": "${EMAIL_PASS}"
      }
  }





  
  